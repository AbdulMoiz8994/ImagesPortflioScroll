export const defaultLocale = "gr" as const;

// export const locales = ["en", "ar", "es", "de", "zh", "he", "gr"] as const;
export const locales = ["gr", "en"] as const;
export const rtlLocales = ["ar", "he"] as const;

// need to customize later
export const languageNames = {
  gr: 'Greek',
  en: "English",
};
