import { Text } from '@chakra-ui/react'
import React from 'react'

const SectionHeading = ({ text }) => {
  return <Text fontWeight="semibold" fontSize="2xl">{text}</Text>
}

export default SectionHeading
